import type { Confession, Letter, OpenWhenEntry } from "@/lib/types";

/**
 * Letters, Open-When notes and confessions are written by the couple themselves.
 * The seed intentionally stays empty — we never invent anyone's words.
 */
export const seedLetters: Letter[] = [];

export const seedOpenWhen: OpenWhenEntry[] = [];

export const seedConfessions: Confession[] = [];